<p>
  <%=h @word.unit.name %>
</p>
<center>
<% if @fragment %>
  <h1>
    <%= link_to_function h(@word.content), "play(\'#{@audio.filename}\',#{@fragment.start},#{@fragment.end});" %>
  </h1>
<% else %>
  <h1><%= h(@word.content) %></h1>
<% end %>
<div id='navigation'>
  <%= button_to_function('previous', "show_previous();") %>
  <%= button_to_function('next', "show_next();") %>
</div>
</center>

<% for translation in @word.translations -%>
  <p>
  <%= "[#{translation['lang']}]" %>
  <%= translation['content'] %>
  </p>
<% end -%> 

<p>
  <%= link_to "Edit", edit_word_path(@word) %> |
  <%= link_to "Destroy", @word, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", words_path %> |
</p>
